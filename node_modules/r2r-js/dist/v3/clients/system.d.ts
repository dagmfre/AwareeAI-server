import { r2rClient } from "../../r2rClient";
import { WrappedGenericMessageResponse, WrappedServerStatsResponse, WrappedSettingsResponse } from "../../types";
export declare class SystemClient {
    private client;
    constructor(client: r2rClient);
    /**
     * Check the health of the R2R server.
     */
    health(): Promise<WrappedGenericMessageResponse>;
    /**
     * Get the configuration settings for the R2R server.
     * @returns
     */
    settings(): Promise<WrappedSettingsResponse>;
    /**
     * Get statistics about the server, including the start time, uptime,
     * CPU usage, and memory usage.
     * @returns
     */
    status(): Promise<WrappedServerStatsResponse>;
}
